<div class="jumbotron-overlay">
  <div class="jumbotron text-center">
    <h1>Pumpkin Hub</h1>
    <p>
      Fascinating fall fun for families!
    </p>
  </div>
</div>

<div class="container post-container">
  {{#each model as |post|}}
    {{#link-to 'post' post.id}}
      {{post-tile post=post}}
    {{/link-to}}
  {{/each}}
</div>
